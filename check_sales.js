const admin = require('firebase-admin'); const sa = require('./functions/serviceAccountKey.json'); admin.initializeApp({credential: admin.credential.cert(sa)}); const db = admin.firestore(); db.collection('sales').where('date', '>=', '2025-12-01').get().then(snap => { console.log('Total sales:', snap.size); snap.forEach(doc => { const d = doc.data().date; if (d && d.startsWith('2026-02') || d.startsWith('2026-03')) { console.log(doc.id, doc.data().date, doc.data().value); } }); process.exit(0); });
